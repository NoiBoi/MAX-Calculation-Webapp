/** Fixed four-point-probe geometric correction used by this calculation version. */
export const FOUR_POINT_PROBE_CORRECTION_FACTOR = 4.532 as const;

/** Stored with electrical records so a future convention change cannot reinterpret history. */
export const EMI_ELECTRICAL_CALCULATION_VERSION = "1.0.0-four-point-probe-simon" as const;

